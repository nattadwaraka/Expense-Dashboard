# Expense Dashboard – same dependencies on Windows, macOS, and Linux.
# Use Python 3.10+ (64-bit). Install inside a venv on all platforms, e.g.:
#   python -m venv venv  &&  venv\Scripts\activate  (Windows)  or  source venv/bin/activate  (macOS/Linux)
# SQLite is built into Python; no extra database drivers are required.

fastapi>=0.110.0,<0.116.0
uvicorn[standard]>=0.29.0,<0.36.0
sqlalchemy>=2.0.30,<2.1.0
pydantic>=2.7.0,<3.0.0
email-validator>=2.0.0,<3.0.0
pytest>=8.0.0,<9.0.0
httpx>=0.27.0,<0.29.0
